version: 2
jobs:
  build:
    docker:
      # specify the version
      - image: circleci/golang:1.13

    working_directory: /go/src/github.com/spikeekips/mitum
    steps:
      - checkout

      - run:
         name: update modules
         command: go get -v -t -d ./...

      - run:
         name: unit test
         command: go test -race -tags test -timeout 1m -v ./...
